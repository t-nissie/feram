# -*-Makefile-*- for fft_old_check
##
FC=gfortran
FFLAGS=-Wall -O3 -fopenmp -ffree-form -ffree-line-length-none
CPPFLAGS=-I/usr/local/include
LDFLAGS=-lfftw3 -lfftw3_omp

fft_old_check: fft_old_check.o
	$(FC) $(FFLAGS) -o $@ $^ $(LDFLAGS)
clean:
	rm -f core* fft_old_check fft_old_check.o
