#!/bin/sh
#PJM -L "rscgrp=short"
#PJM -L "node=1"
#PJM -L "elapse=2:00:00"
#PJM -j
export PJM_JOBNAME=timing.FX10
export FLIB_FASTOMP=TRUE
for i in 2 4 8 10 12 14 16; do
    PARALLEL=$i OMP_NUM_THREADS=$i /usr/bin/time -o timing.dat -f "$i %e %U %S %R" -a ../feram forward.feram
done
