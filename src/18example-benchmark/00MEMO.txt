SR16000 timing
     $ cat time.SR16000
     time ../feram forward.feram
     $ for i in `jot -w '%02d' 64`; do submit EM general -pt $i -J time$i -exec time.SR16000; done

FX10 profiler
     $ pjsub profiling.FX10
     $ fipppx -Ibalance,cpu,hwm -l 40  -A -d profiling > profiling.FX10.PROFILE
